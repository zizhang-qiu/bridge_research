# train
checkpoint_path: null
act_device: "cuda"
train_device: "cuda"
num_threads: 10
num_games_per_thread: 100

policy_lr: !!float 1e-5
value_lr: !!float 1e-4
seed: !!int 42
burn_in_frames: !!int 80000
buffer_capacity: !!int 800000
num_epochs: 10000
epoch_len: 1000
sample_batch_size: 1024
entropy_ratio: !!float 0.01
clip_eps: !!float 0.2
sync_freq: 50
max_grad_norm : !!float 0.5

# eval
num_eval_deals: 50000
num_eval_threads: 10
eval_device: "cuda"

# save
save_dir: "a2c"
top_k : 5
